import 'package:flutter/material.dart';

class RestockToolPage extends StatelessWidget {
  const RestockToolPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Restock Helper')),
      body: const Center(child: Text('Restock Helper Page')),
    );
  }
}
